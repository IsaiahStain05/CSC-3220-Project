# CSC-3220-Project: Predicting Income Class from Census Data

## NOTE: Please open dataCleaningAndModelTesting.R FROM YOUR REPOSITORY DIRECTORY. This automatically sets the working directory in R Studio to that directory.  

  ### Code to run the app (from cloned repository directory, not CensusApplication Directory):  
  library(shiny)  
  runApp("CensusApplication")
